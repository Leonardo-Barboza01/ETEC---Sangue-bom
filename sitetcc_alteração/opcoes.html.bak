<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sangue Bom</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f5e9e9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
        }

        .central-box {
            background-color: #e0a4a4;
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            width: 400px; /* Largura ajustada */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            display: flex;
            flex-direction: column; /* Organiza os botões em coluna */
            gap: 15px; /* Espaçamento entre os botões */
        }

        .btn {
            background-color: #8b1b1b;
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            border-radius: 20px;
            cursor: pointer;
            width: 100%; /* Botões ocupam toda a largura da div */
            transition: background-color 0.3s; /* Transição ao passar o mouse */
        }

        .btn:hover {
            background-color: #6c1414; /* Cor ao passar o mouse */
        }

        .btn.voltar {
            background-color: white;
            color: #8b1b1b;
            border: 2px solid #8b1b1b;
            padding: 10px 0; /* Ajusta o padding para deixar o botão mais alto */
            margin-top: 10px; /* Espaço extra acima do botão VOLTAR */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="central-box">
            <button class="btn consulta" onclick="window.location.href='consulta.html'">CONSULTA</button>
            <button class="btn dicas" onclick="window.location.href='dicas.html'">DICAS</button>
            <button class="btn voltar" onclick="window.location.href='inicio.html'">VOLTAR</button>
        </div>
    </div>
</body>
</html>
