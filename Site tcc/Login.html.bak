<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Georgia', serif;
            margin: 0;
            padding: 0;
            color: #3B3B3B;
			background-color: #ff4d4d;
        }
        .page {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100vh;
            background-color:#F08080;
        }
        .formLogin {
            display: flex;
            flex-direction: column;
            background-color: #fff;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 10px 10px 40px rgba(0, 0, 0, 0.3);
            gap: 5px;
            max-width: 400px;
            width: 90%;
        }
        .formLogin h1 {
            margin: 0;
            font-weight: bold;
            font-size: 2.3em;
            color: #8b1b1b; /* Título em vermelho escuro */
            text-align: center;
        }
        .formLogin p {
            font-size: 15px;
            color: #666;
            margin-bottom: 25px;
            text-align: center;
        }
        .formLogin label {
            font-size: 14px;
            font-weight: 600;
            color: #3B3B3B;
        }
        .formLogin input {
            padding: 12px;
            font-size: 14px;
            border: 1px solid #ccc;
            margin-bottom: 20px;
            border-radius: 20px;
            transition: all linear 160ms;
            outline: none;
        }
        .formLogin input:focus {
            border: 1px solid #8b1b1b; /* Borda de foco em vermelho escuro */
        }
        .formLogin a {
            font-size: 13px;
            color: #8b1b1b; /* Link em vermelho escuro */
            text-decoration: none;
            text-align: center;
            display: block;
        }
        .btn {
            background-color: #8b1b1b; /* Vermelho escuro */
            color: #fff; 
            font-size: 16px;
            font-weight: bold;
            border: none;
            cursor: pointer;
            width: 100%;
            height: 50px;
            border-radius: 20px;
            transition: all linear 160ms;
        }
       

        .btn.voltar:hover {
            background-color: #fff; /* Fundo vermelho */
            color: #fff; /* Texto branco */
            transform: scale(1.1); /* Aumenta levemente o botão */
        }
    </style>
</head>
<body>
    <div class="page">
        <form method="POST" class="formLogin" onsubmit="return handleLogin(event)">
            <h1>Login</h1>
            <p>Digite os seus dados de acesso no campo abaixo.</p>
            <label for="email">E-mail</label>
            <input type="email" id="email" placeholder="Digite seu e-mail" autofocus required />
            <label for="password">Senha</label>
            <input type="password" id="password" placeholder="Digite sua senha" required />
            
			<a href="inicio.html">Acessar sem Login</a>
			<br>
            <input type="submit" class="btn" value="Acessar">
        </form>
    </div>

    <script>
        function handleLogin(event) {
            event.preventDefault(); // Impede o envio do formulário
            const email = document.getElementById('email').value;
            const username = email.split('@')[0]; // Extrai o nome de usuário do e-mail
            window.location.href = 'inicio.html';
        }
    </script>
</body>
</html>
